#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	int n, m;
	while(1){
		cin >> n >> m;
		if(n==0 && m==0) break;
		cout << n+m << "\n";
	}
}
